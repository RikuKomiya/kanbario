# Claude Code personal settings
.claude/settings.local.json

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/

# Swift Package Manager
.swiftpm/
Packages/
Package.pins
Package.resolved
.build/

# macOS
.DS_Store

# libghostty / GhosttyKit artifacts (produced by scripts/ensure-ghosttykit.sh)
vendor/libghostty/
/GhosttyKit.xcframework
# Build intermediates inside the ghostty submodule (do not let zig-cache /
# macos/GhosttyKit.xcframework leak as untracked files in git status)
ghostty/zig-cache/
ghostty/zig-out/
ghostty/macos/GhosttyKit.xcframework/

# App runtime data (never commit user data)
*.app/Contents/Resources/bin/claude.real
*.profraw
